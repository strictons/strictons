<section class="contact-cta" aria-labelledby="contact-cta-heading">
  <div class="contact-cta-card">
    <div>
      <h2 id="contact-cta-heading">Make every guest feel welcome.</h2>
      <p>
        Let’s create a pocket guide<br class="contact-cta-break" /> for your hotel.
      </p>
    </div>
    <a class="contact-cta-button" href="/contact">
      Get in touch <span aria-hidden="true">↗</span>
    </a>
  </div>
</section>

<style>
  .contact-cta {
    padding-block: 48px;
  }

  .contact-cta-card {
    position: relative;
    display: flex;
    width: min(1160px, calc(100% - 96px));
    margin-inline: auto;
    padding: clamp(40px, 5vw, 64px);
    background: #151515;
    box-shadow: 0 24px 60px rgb(0 0 0 / 14%);
    align-items: center;
    justify-content: space-between;
    gap: 40px;
  }

  h2 {
    color: #d4d0c9;
    font-family: var(--font-serif);
    font-size: clamp(1rem, 1.35vw, 1.25rem);
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  p {
    margin-top: 0.75rem;
    color: #f3dfba;
    font-family: var(--font-serif);
    font-size: clamp(1.5rem, 2.2vw, 2.125rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    line-height: 1.2;
    background: linear-gradient(
      105deg,
      #b89160 8%,
      #f5ddb0 32%,
      #fff7df 43%,
      #c89d61 54%,
      #f5ddb0 72%,
      #b89160 92%
    );
    background-position: 50% 50%;
    background-size: 220% 100%;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .contact-cta-button {
    display: inline-flex;
    min-height: 46px;
    padding: 10px 19px;
    flex-shrink: 0;
    border: 1px solid #c89d61;
    color: #f7efe7;
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.045em;
    text-decoration: none;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    transition:
      background 180ms ease,
      border-color 180ms ease,
      color 180ms ease;
  }

  .contact-cta-button:hover {
    border-color: #f3dfba;
    background: #f3dfba;
    color: #29171b;
  }

  .contact-cta-button:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 5px;
  }

  .contact-cta-break {
    display: none;
  }

  @media (max-width: 760px) {
    .contact-cta {
      padding-block: 24px 48px;
    }

    .contact-cta-card {
      width: calc(100% - 40px);
      padding: 40px 24px;
      text-align: center;
      flex-direction: column;
      align-items: center;
      gap: 24px;
    }

    h2 {
      font-size: 0.875rem;
    }

    .contact-cta-break {
      display: block;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .contact-cta-button {
      transition: none;
    }
  }
</style>
